#include<stdio.h>
int main()
{
    int length,bredth;
    printf("the length of the rectangle is \n");
    scanf("%d",&length);
    printf("the bredth of the rectangle is \n");
    scanf("%d",&bredth);
    int area;
    area=length*bredth;
    printf("the area of rectangle is %d",area);
    scanf("%d",&area);
    return 0;
}
